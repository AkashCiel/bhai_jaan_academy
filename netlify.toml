[build]
  publish = "frontend"
  command = "echo 'No build command needed for static site'"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[dev]
  command = "echo 'No dev command needed'"
  port = 8888
  publish = "frontend" 